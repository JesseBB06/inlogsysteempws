publieke sleutel (moet toegevoegd worden in de html) 6LeuziYaAAAAAPEnRY5B1KYe9R6LxMgo45RpKq6w
prive sleutel (alleen voor ons) 6LeuziYaAAAAAAzlRdcrAM7yWRmLSxiSGfCLlKfA


<script src='https://www.google.com/recaptcha/api.js' async defer ></script> (dit moet toegevoegd worden in de pagina)
<div class="g-recaptcha" data-sitekey="6LeuziYaAAAAAPEnRY5B1KYe9R6LxMgo45RpKq6w"></div> (deze waar we willen dat de recaptcha toegevoegd wordt (ergens bij de register en/of login i guess)

(dit moet ook ergens op de website staan)
<?php
 
  if(isset($_POST['g-recaptcha-response']) && !empty($_POST['g-recaptcha-response']))
  {
        $secret = '6LeuziYaAAAAAAzlRdcrAM7yWRmLSxiSGfCLlKfA';
        $verifyResponse = file_get_contents('https://www.google.com/recaptcha/api/siteverify?secret='.$secret.'&response='.$_POST['g-recaptcha-response']);
        $responseData = json_decode($verifyResponse);
        if($responseData->success)
        {
            $succMsg = 'De recaptcha is gelukt.';
        }
        else
        {
            $errMsg = 'Recaptcha is niet gelukt, probeer het opnieuw.';
        }
   }
?>
